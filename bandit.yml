# File: bandit.yaml

excluded_paths:
  - 'tests'                # don’t scan tests

skips:
  - 'B101'                 # skip assert usage warnings

tests:
  - 'B301'                 # only medium+ severity checks
  - 'B302'
  - 'B303'

level: 'high'           # report only HIGH severity
confidence: 'high'         # report only HIGH confidence
